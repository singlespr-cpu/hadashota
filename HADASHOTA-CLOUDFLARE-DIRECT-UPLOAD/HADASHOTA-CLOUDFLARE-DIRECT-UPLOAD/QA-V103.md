# V103 — Premium Position + Data Fix

1. Removed the actual red/green LIVE pill from the main top header.
   Other contextual live indicators (lead/latest) are untouched.

2. Moved "מה באמת קורה עכשיו?" + "מאז שנכנסת" from the bottom of the page
   to immediately below the smart intelligence bar and above search/filters/feed.

3. Fixed the premium panel data bug:
   V101/V102 called non-existent formatRelativeTime(), which could abort rendering.

4. Premium intelligence now runs directly inside the main newsroom render cycle.
   It updates whenever the news data renders, including background refreshes.

5. Uses latestReportAt/reportCount/cluster reports, matching the site's real data model.

6. Added quiet-news fallback for topic display.

7. Fixed "מאז שנכנסת" so its timestamp is not overwritten on every 30-second refresh.

No unrelated site features were changed.
