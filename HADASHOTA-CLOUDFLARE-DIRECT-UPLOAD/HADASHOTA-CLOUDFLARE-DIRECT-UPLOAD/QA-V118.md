# QA V118

## Changes
- Removed the residual decorative stripe from the featured lead-story container.
- Disabled both `::before` and `::after` legacy decoration layers on the lead story.
- Added a hard reset on the rotating lead headline (`h2`, link and title span) so no border/background/text-decoration/pseudo layer can paint a line through it.
- Bumped CSS, JS and service-worker asset versions from `116.0.0` to `118.0.0` so browsers fetch the corrected build instead of retaining older assets.

## Checks
- `node --check app.js`
- `node --check _worker.js`
- Verified index references `styles.css?v=118.0.0` and `app.js?v=118.0.0`.
- Verified service worker registration/version `118.0.0`.
