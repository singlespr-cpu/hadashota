# V110 — Production Stability QA

## 1. Buttons / interactions
- 36 static buttons with IDs audited.
- Unreferenced button IDs in app.js: none.
- Duplicate HTML IDs: none.
- Added a delegated safety net for the dynamic premium controls/settings.
- Active important/location filters are reapplied after feed rebuilds.

## 2. 30-second automatic refresh
- Foreground cadence: 30 seconds.
- `force=1` is still sent on scheduled refreshes, so Worker collection reaches source publishers.
- Silent background refresh remains enabled; existing content stays clickable.
- Fixed a timer-loss edge case: if the 30-second timer fires during another refresh,
  it reschedules in 2 seconds instead of silently stopping forever.
- Failure paths also guarantee that the main automatic loop remains scheduled.
- Responsive merger continues yielding to the browser main thread.

## 3. Mobile horizontal overflow
- html/body clipped to viewport width.
- All principal grid/shell/card containers receive min-width:0/max-width:100%.
- Explicit horizontal scrolling remains only on intended strips/filters.
- 700px mobile shell uses symmetric 10px side margins.

## 4. Production checks
- Node syntax check: app.js / _worker.js / sw.js.
- Version/API marker logic cleaned up for V110.
- Cache namespaces bumped.
- Existing ads/admin, source collection, image/credit logic and premium tools retained.
