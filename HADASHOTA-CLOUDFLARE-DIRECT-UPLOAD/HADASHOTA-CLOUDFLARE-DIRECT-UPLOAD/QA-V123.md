# V123 — True Web Push

- Cloudflare Cron scheduled handler added (1 minute).
- SQLite Durable Object stores subscriptions, generated VAPID keypair and last pushed lead.
- VAPID private key is generated server-side and never shipped to browser code.
- Browser creates a real PushSubscription and sends it to the Worker.
- Service Worker handles background push and shows notification even with no open site tab.
- iPhone guard explains Home Screen installation requirement.
- Foreground-only duplicate notifications disabled.
- V122 right-side back-to-top arrow retained.
- V121 Google verification and SEO retained.
