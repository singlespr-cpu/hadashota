# QA V117

## Changes
- Removed any possible decoration/pseudo-element paint from the rotating lead headline layers to prevent the line/strike artifact.
- Reworked lead headline sizing: character count + word count, with `normal`, `medium`, `long`, `xlong` tiers and a post-layout height safety check.
- Polished hero spacing and CTA rhythm.
- Made feed imagery image-first: the first 12 visible update cards resolve their original article image immediately; the rest stay intersection-observer lazy loaded.
- Kept `/api/source-image` as the preferred fallback before contextual/open-media imagery.
- Improved image loading surfaces and feed card spacing.

## Checks performed
- `node --check app.js` — passed.
- `node --check _worker.js` — passed.

## Manual browser QA recommended after deploy
1. Rotate through several lead stories, including 1-line, 2-line and very long titles; verify no line crosses title glyphs.
2. Verify long titles step down rather than clipping/overflowing.
3. Hard refresh and confirm first visible cards populate article thumbnails quickly.
4. Verify cards without a usable source image retain a neutral fallback rather than breaking layout.
5. Verify mobile widths at ~390px and ~430px.
